# خارطة الطريق ومتابعة المهام البرمجية (System Roadmap & Milestones)
## منظومة eShamikh Cloud & Services Ecosystem

---

## 1. جدول التقدم الإجمالي (Master Progress Tracker)

| **Task 0** | **إنشاء مجلد التوثيق والسياق (`.md/`) بملفاته العشرة** | 🟢 **مكتملة 100%** | مراجعة الملفات وتلقي أمر `next step` |
| **Task 1** | **قاعدة بيانات سوبابيس الموحدة (Supabase Schema & RLS)** | 🟢 **مكتملة 100%** | مراجعة ملفات الهجرة وتلقي أمر `next step` |
| **Task 2** | **طبقة الحماية ومكتبة التوقيع ومركز المصادقة (`/auth`)** | 🟢 **مكتملة 100%** | فحص الأمان وتلقي أمر `next step` |
| **Task 3** | **واجهات التصميم الأبيض الفاخر وبوابة الموافقة** | 🟢 **مكتملة 100%** | بوابة الموافقة الإلزامية على التصميم والواجهات |
| **Task 4** | **محرك الفوترة الشهرية، تسوية كي كارد، وتكامل eTrack** | ⚪ قيد الانتظار | صدور الموافقة على الواجهات وأمر `next step` |

---

## 2. تفاصيل المراحل وقوائم الفحص (Checklists)

### ✅ المهمة التمهيدية (Task 0): مجلد التوثيق والسياق (`.md/`)
- [x] إنشاء ملف `instruction.md` (قواعد وسلوك الوكيل وبوابات التوقف الصارمة).
- [x] إنشاء ملف `memory.md` (سجل الحالة وتاريخ القرارات المعمارية).
- [x] إنشاء ملف `architecture.md` (طوبولوجيا النطاقات ومعمارية Monorepo).
- [x] إنشاء ملف `possibleThreats.md` (مصفوفة التهديدات السيبرانية والمالية).
- [x] إنشاء ملف `security.md` (خوارزميات HMAC-SHA256 والترويسات والوسيط).
- [x] إنشاء ملف `database.md` (مخطط PostgreSQL، المشغلات، وسياسات RLS).
- [x] إنشاء ملف `designSystem.md` (مواصفات التصميم الأبيض الفاخر والخط العربي).
- [x] إنشاء ملف `businessLogic.md` (قواعد الاشتراك الثابت 25,000 د.ع. وبروتوكول الحجب الثلاثي وسداد كي كارد).
- [x] إنشاء ملف `apiContract.md` (عقود APIs للمصادقة وتتبع الشحنات والمتاجر).
- [x] إنشاء ملف `roadmap.md` (خارطة الطريق ومتابعة التنفيذ).

---

### ✅ المهمة 1 (Task 1): قاعدة بيانات سوبابيس (Supabase Schema)
- [x] إعداد ملف الهجرة الموحد `supabase/migrations/complete_schema.sql` والملفات المجزأة.
- [x] إنشاء مشروع سوبابيس المخصص والمستقل `eServices` (`ojzemkznqoxotryrecbc`).
- [x] إنشاء جداول `profiles`, `services`, `user_services`, `stores`, `store_orders`, `monthly_invoices`, `security_audit_logs`.
- [x] برمجة المشغل التلقائي لمزامنة مستخدمي `auth.users` بجدول `profiles`.
- [x] برمجة دالة توليد الفواتير الشهرية بقيمة الاشتراك الثابت (25,000 د.ع.) آلياً.
- [x] برمجة دالة تنفيذ بروتوكول الحجب الثلاثي (Tier 1, 2, 3) حسب انقضاء فترة السماح.
- [x] تفعيل وتطبيق سياسات RLS المحكمة لكل دور مستخدم مع فهارس تسريع الاستعلامات.
- [x] إضافة البيانات الأولية للخدمات السحابية الأساسية (eStore, eTrack, eForm, eLink).

---

### ✅ المهمة 2 (Task 2): طبقة الحماية ومركز المصادقة (`/auth`)
- [x] برمجة مكتبة العميل المستقلة `lib/security/signer.ts` لتوليد ترويسات `X-Signature` و `X-Timestamp`.
- [x] برمجة وسيط السيرفر `middleware.ts` لفحص التوقيع والانحراف الزمني ومقارنة التوقيع المقاومة لهجمات التوقيت (`timingSafeEqualHex`).
- [x] برمجة الرد الموحد برمز `403` في حال التلاعب بالطلب وتسجيل الواقعة في `security_audit_logs` في سوبابيس.
- [x] بناء شاشات الدخول والتسجيل وإدارة الملف الشخصي في مسار `/auth` وتثبيت كوكيز النطاق المشترك `Domain=.eshamikh.com`.
- [x] توفير مختبر أمان تفاعلي حي لاختبار الطلب السليم واختبار التلاعب واختبار تكرار الختم المنتهي.

---

### 🟢 المهمة 3 (Task 3): واجهات التصميم الأبيض الفاخر (Luxury White UI)
- [x] بناء واجهة البوابة السحابية المركزية `services.eshamikh.com`.
- [x] بناء واجهة مركز المصادقة وإدارة الحساب ومختبر درع الأمان `services.eshamikh.com/auth`.
- [x] بناء واجهة لوحة تحكم متجر التاجر `estore.eshamikh.com/dashboard` مع محاكي الحجب الثلاثي ونافذة سداد كي كارد.
- [x] بناء واجهة محرك التتبع اللوجستي المباشر `services.eshamikh.com/track` و `/services/etrack`.
- [x] بناء واجهات استعراض الأدوات الذكية `eForm` و `eLink`.
- [x] استيفاء كافة معايير الفخامة: خلفية عاجية `#FAFAFA`، أسطح بيضاء `#FFFFFF`، خط `IBM Plex Sans Arabic`، وظلال مجهرية.
- [x] **بوابة الموافقة الإلزامية:** التوقف فوراً وطرح السؤال المعياري حصراً:
  > *"هل عجبتك الواجهات والتصميم حتى أكمل ربطه بقاعدة البيانات والمنطق البرمجي؟"*
- [ ] انتظار صدور الموافقة الصريحة.

---

### ⏳ المهمة 4 (Task 4): الربط والفوترة وتسوية كي كارد
- [ ] ربط الواجهات بقواعد بيانات Supabase مع احترام سياسات RLS والتوقيع الأمني.
- [ ] برمجة معالج الفواتير الشهرية التلقائي وتطبيق بروتوكول الحجب الثلاثي (Tier 1 يوم 6، Tier 2 يوم 8، Tier 3 يوم 15).
- [ ] بناء واجهة سداد الفاتورة وإدخال رقم إشعار كي كارد ورفع صورة الوصل.
- [ ] إطلاق مسارات خدمة التتبع اللوجستي `eTrack`.
